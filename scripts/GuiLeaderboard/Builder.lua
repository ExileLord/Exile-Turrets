--Responsible for initially building the gui leaderboard that is displayed on screen
local Builder = {}
local Names = require("scripts.GuiLeaderboard.Names")
local Styles = require("lib.Styles")

local create_leaderboard_frame
local add_table
local add_table_header
local add_table_row
local add_table_cell
local add_table_rank_cell
local add_table_


function create_leaderboard_frame(leaderboard)
end

function add_table(leaderboard, frame)
end

function add_table_header(leaderboard, table)
end

function add_table_row(leaderboard, table, entry)
end

function add_table_cell(leaderboard, table, row, type)
end







return Builder